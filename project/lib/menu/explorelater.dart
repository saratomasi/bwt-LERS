import 'package:flutter/material.dart';

class ExploreLater extends StatelessWidget {
  const ExploreLater({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}